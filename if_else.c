#include<stdio.h>
// Created by github.com/theriturajps | @RituRajPS <----
int main()
{
    int age;
    printf("Enter your age : ");
    scanf("%d", &age);
    if(age>=18){
        printf("You are ready to vote");
    } else {
        printf("You are not ready to vote");
    }
    return 0;
    // @RituRajPS <----
}

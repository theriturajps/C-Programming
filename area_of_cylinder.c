#include<stdio.h>

int main()
{
    int rad, height;

    float pi = 3.141592653589793238462643;

    printf("Enter the radius :");
    scanf("%d", &rad);

    printf("Enter the Height :");
    scanf("%d", &height);

    float volume = pi*rad*rad*height;
    float area = 

    printf("The volume of cylinder is %f", volume);
    return 0;
}